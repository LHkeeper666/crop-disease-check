spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agriculture_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: agri_root_2026

  data:
    redis:
      host: localhost
      password:

  rabbitmq:
    host: localhost
    username: agri
    password: agri_mq_2026
    virtual-host: /

  elasticsearch:
    uris: http://localhost:9200

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: agri-monitor

file:
  upload-path: /data/agriculture/uploads/images/
