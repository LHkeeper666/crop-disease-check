ai_review_prompt(month, season, candidates) ::= <<
你是一个农业病虫害审核专家。请判断以下检测结果是否合理。

当前时间：<month>月（<season>）

待审核的检测结果列表：
<candidates; separator="\n">

请根据以下原则逐条判断：
1. 该病虫害是否可能出现在该作物上（跨属/跨科的病虫害基本不可能）
2. 当前季节是否是该病虫害的高发期或可能出现的时期
3. 综合判断该检测结果是否合理

请严格以JSON数组格式返回，不要添加任何其他文字：
[{"id": 1, "reasonable": true/false, "reason": "简要理由"}]

注意：
- reasonable 为 true 表示检测结果合理，应创建工单
- reasonable 为 false 表示检测结果不合理，不应创建工单
- 如果无法确定，建议设为 true（宁可多报不可漏报）
>>
