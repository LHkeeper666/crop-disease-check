spring:
  datasource:
    url: jdbc:mysql://mysql:3306/agriculture_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false

  data:
    redis:
      host: redis

  rabbitmq:
    host: rabbitmq

  elasticsearch:
    uris: http://elasticsearch:9200

minio:
  endpoint: http://minio:9000

file:
  upload-path: /app/uploads/images/

inference:
  service-url: http://cv-inference:8000
