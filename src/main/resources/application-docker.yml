spring:
  datasource:
    url: jdbc:mysql://mysql:3306/agri_monitor?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false

  data:
    redis:
      host: redis

  rabbitmq:
    host: rabbitmq

  elasticsearch:
    uris: http://elasticsearch:9200

minio:
  endpoint: http://minio:9000

file:
  upload-path: /app/uploads/images/
