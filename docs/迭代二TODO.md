# 迭代二 TODO 清单

> 整理日期：2026-06-14
> 状态说明：⬜ 待开始 | 🔄 进行中 | ✅ 已完成

---

## 一、检测阈值与工单联动逻辑

检测结果的置信度必须 **>= 前端设置的检测阈值**，才会触发自动工单生成。低于阈值的结果仅保留历史记录。

### 1.1 工单展示范围

满足阈值生成的工单需同步出现在：

| 模块 | 页面 | 展示内容 |
|------|------|----------|
| 遥测总览 | `/dashboard` | 工单列表 + 七日趋势 |
| 报警模块 | — | 实时报警通知 |
| 智能工单 | `/workorders` | 状态为「待处理」 |
| 农情报表 | `/reports` | 七日趋势 + 统计报表 |

- [ ] 实现阈值判定逻辑
- [ ] 上述四处工单展示联动

### 1.2 重复工单判定

- **同一地点 + 相同问题** → 不重复生成（避免堆积）
- **不同地点 + 相同问题** → 各自独立生成（分别跟踪）

- [ ] 实现重复工单判定逻辑

---

## 二、图像检测页面

- [ ] **图片尺寸固定**：上传后固定显示容器大小，图片自适应缩放，防止「开始检测」按钮被推出可视区域
- [ ] **页面高度稳定**：不因图片尺寸变化改变组件整体高度

---

## 三、页面切换白屏问题

**受影响页面**：农情报表 (`/reports`)、图像检测、实时监控

**表现**：切出这些页面时概率性白屏，需 `Ctrl+Shift+R` 强制刷新；页面切换动画未完成时立即切换路由，复现率更高。

**方向**：排查组件卸载/挂载生命周期，考虑路由级 `keep-alive` 或过渡动画中断处理。

- [ ] 修复三页面切换白屏
- [ ] 快速切换路由兼容

---

## 四、智能工单状态规范化 (`/workorders`)

### 4.1 问题

存在异常状态工单（如「辣椒疫病蔓延预警-C区」），UI 样式与其他工单不一致，且曾导致遥测总览与农情报表的七日趋势数据不一致。

### 4.2 规范状态（仅限四种）

| 状态 | 说明 |
|------|------|
| 待处理 | 新工单，等待指派 |
| 处理中 | 已接手，正在处理 |
| 已完成 | 问题已解决 |
| 忽略 | 评估后不处理 |

- [ ] 统一所有工单为四种状态
- [ ] 修复异常工单 UI
- [ ] 验证遥测总览与农情报表七日趋势一致性

---

## 五、农情报表 (`/reports`)

### 5.1 病害/虫害分布数据源

数据来源应为 **当日未被忽略的工单**（待处理 + 处理中 + 已完成），忽略状态不参与统计。

- [ ] 修正病害分布数据源
- [ ] 修正虫害分布数据源

### 5.2 生成日报逻辑

当前问题：生成日报后数据大屏内容被清空，且没有任何输出。

- [ ] 生成日报后保留数据大屏内容
- [ ] 未接入 API 时禁止生成（按钮置灰/提示）
- [ ] 接入 API 后生成可下载文档（含完整数据分析，非简单罗列）

---

## 六、七日趋势数据丰富

当前仅有当天数据（两个散点），无法连成曲线。

- [ ] 数据持久化后补充历史日期数据点
- [ ] 七日趋势图展示连续折线走势

---

## 七、摄像头与区域逻辑修正

### 7.1 正确配置

| 区域 | 摄像头编号 | 小计 |
|------|-----------|------|
| A区  | 1号、2号、3号 | 3 |
| B区  | 1号、2号、3号 | 3 |
| C区  | 1号、2号、3号 | 3 |
| **合计** | | **9** |

- [ ] 删除 D区、E区等多余区域
- [ ] 每区域修正为 3 个摄像头

### 7.2 状态真实性

摄像头状态必须与实际硬件一致：在线 / 故障 / 未连接。当前大量摄像头统一显示为「在线」，与实际不符。

- [ ] 修改遥测总览 (`/dashboard`) 摄像头状态
- [ ] 修改设备管理 (`/devices`) 摄像头状态
- [ ] 修改实时监控摄像头状态

### 7.3 热力图与监控联动

数据链路：**摄像头检测 → 工单生成 → 热力图渲染 → 监控展示**

- [ ] 热力图数据源与工单逻辑匹配
- [ ] 实时监控与热力图数据匹配

---

## 八、设备管理页面刷新问题 (`/devices`)

admin 刷新浏览器后用户管理模块无内容显示。数据可能未丢失，但加载时序/接口调用有问题。

- [ ] 排查并修复刷新后数据加载问题

---

## 九、文档目录整理

`docs/迭代二内容/` 下存在三份迭代一文档，需迁移至 `docs/迭代一内容/`：

- `迭代一冲刺待办列表.md`
- `迭代一冲刺计划会议纪要.md`
- `迭代一迭代回顾会议纪要.md`

- [ ] 执行文件迁移


## 十、我自己写的原话

1.图像检测图片上传之后页面中需要固定一下大小，有些图片上传完之后“开始检测”的按钮在页面下方点不到，整个web页面回因为图片大小而改变组件的高度

2.检测阈值是和是和切片处理有关，只有大于等于前端设置的检测阈值才会把问题自动整理成工单并且在遥测总览显示，在报警中显示，在智能工单里显示（状态为：待处理），在http://localhost:3000/reports（农情报表）和http://localhost:3000/dashboard（遥测总览）中的七日里显示，报表什么的逻辑也是体现（而且这个如果摄像头检测后生成工单后需要加入重复工单判定的判定，不能说同一个地方搞一堆工单，还有就是肯定存在多个地方有一样的问题这个逻辑是要的，但是同一个地方重复时不要的）

3.图像检测完之后切换到别的模块发现页面空白没有内容只能通过ctrl+shift+r强制刷新，甚至说page切换太快（页面切换动画没有执行完就换页面也是同样的问题），我发现其他还好，问题应该在 农情报表和图像检测和实时监控这个三个页面里（切出去有问题，会让前端没显示）

4.http://localhost:3000/workorders（智能工单）里有一个辣椒疫病蔓延预警-C区有问题这个逻辑不对（应该只有四个状态）：待处理、处理中、已完成、忽略，他那个ui都和别的不一样这个应该是有人改前端的时候没注意----我记得之前就是因为这个倒是遥测总览的七日趋势和农情报表的七日趋势显示不一致

5.http://localhost:3000/reports（农情报告）的病害分布和虫害分布数据来源应该是现有的没有被忽略的工单（当日的完成的处理中的待处理的三种状态的数据支持这两个模块）

6.http://localhost:3000/reports（农情报表）的生成日报有逻辑问题，生成后数据大屏的内容消失，应该保留，还有就是没有输出，这个日报应该加个逻辑是没有接入api是不允许生成，接入后是可以生成文档（作为输出，和那个Excel一样，但是日报的话是有分析有别的内容，是实实在在的分析）

7.七日趋势的数据现在只有当天的也就是当日的日期，最后我们完成所有内容持久化后应该加一些其他日子的数据，让这个数据图不再是单独的两个散点，现在只有当天的两个散点连不成曲线现在

8.摄像头的逻辑有问题应该是A区有123号摄像头，B区123，C区123，总计九个，现在是有D区？有E区？而且每个区域应该有3个，还有就是监控要和热力图匹配，热力图是和工单逻辑匹配的，还有就是所有摄像头逻辑应该实际化，故障就是故障，未连接就是未连接，我看到有很多在前端显示的是在线，这个要和实际相符（修改遥测总览和设备管理和实时监控里的逻辑）

9.页面刷新之后，即使身为系统管理员（admin）发现在http://localhost:3000/devices（设备管理）的用户管理是没有任何内容的买这个刷新机制导致了数据未显示（应该不是丢失吧）

10.还有就是我发现$PWD/docs/迭代二内容里放着迭代一的文档，负责文档的同学把逻辑搞清楚一下，这个是不是应该放在迭代一内容的目录下？