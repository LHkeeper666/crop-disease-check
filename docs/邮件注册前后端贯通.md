to 黄博：
  贯通前后端还需要改的地方

  后端需要确认/修改（src/ 下）：

  1. AuthServiceImpl.sendOtp() — 需要实现真实的邮件发送逻辑（目前看接口已定义，确认实现类里是否已接入 JavaMailSender
  或类似邮件服务发真实验证码到邮箱，而不是 mock）
  2. AuthServiceImpl.register() — 注册流程需要验证 OTP 验证码。当前 RegisterDTO 里没有 otp 字段，后端需要：
    - 给 RegisterDTO 加一个 otp 字段
    - 在 register() 实现中校验该 OTP 是否与 Redis/数据库中存储的一致
    - 或者拆成两步：先验证 OTP，再注册
  3. OTP 存储 — sendOtp 生成的验证码需要存到 Redis（推荐设置 5 分钟过期），register 时取出比对
  4. 前端 register() 请求体 — 目前只传了 { email, username, password }。如果后端 RegisterDTO 加了 otp 字段，前端的 auth.register()      
  也需要把 regOtp 传进去。这个是前端需要连带改的，改动很小，就一行 body 加个 otp 参数

  总结改动范围：
  - 前端：register() 函数加一个 otp 参数传递（1 行改动）
  - 后端：RegisterDTO 加 otp 字段 + AuthServiceImpl 实现验证码校验 + 真实邮件发送